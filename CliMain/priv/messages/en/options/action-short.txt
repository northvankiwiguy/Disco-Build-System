-s | --short

  Show an abbreviated version of the action, with only the first portion of
  the action's command string displayed (controlled by the --width flag).
  This is in contrast to the default mode that displays the entire command
  string, wrapped appropriately.
